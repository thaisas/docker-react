sudo: required
services:
  - docker

before_install:
  - docker build -t thaisas/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true thaisas/docker-react npm run test

language: generic
